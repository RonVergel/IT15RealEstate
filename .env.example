# Postgres (local dev)
POSTGRES_DB=realestatecrm
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeMeLocal!

# Optional: override ASPNETCORE_ENVIRONMENT
ASPNETCORE_ENVIRONMENT=Development

# Mailjet (local only) - DO NOT COMMIT real values
Mailjet__ApiKey=
Mailjet__SecretKey=
Mailjet__SenderEmail=

# Initial broker seed credentials (local only)
BrokerCredentials__Email=broker@local.test
BrokerCredentials__Password=StrongLocalPassword123!

# Optional DATABASE_URL; if not set it's derived from POSTGRES_* above
# DATABASE_URL=postgres://postgres:ChangeMeLocal!@db:5432/realestatecrm